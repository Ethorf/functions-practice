// a sliding window is basically a set of array indexes which can be opened wider (or shrunk?) depending on conditions
//common problems are "Longest Unique Substring", "maxSubarraySum" -- that would be consecutive elements in an array
